## Архитектура и подход

Цель: построить мультиагентную систему технического интервью, где видимый диалог ведет Интервьюер, а скрытая рефлексия и контроль качества выполняются отдельными агентами. Система должна сохранять контекст, адаптировать сложность, быть устойчивой к вбросам и формировать полезный финальный фидбэк.

---

## Ключевая идея

Разделить роли на специализированные агенты с четким контрактом и отдельными промптами. Перед каждым ответом пользователю система делает «внутренний круг» обсуждения (Hidden Reflection), после чего Интервьюер формирует финальную реплику.

**Принципы архитектуры:**
- Каждый агент выполняет строго одну функцию (Single Responsibility)
- Внутренние коммуникации — структурированный JSON
- Факты проверяются через внешние источники (Web Search)
- Оценки накапливаются по ходу интервью, а не вычисляются в конце

---

## Компоненты системы

### 1. Topic Planner (скрытый агент, запуск в начале)

**Задачи:**
- Принимает вводные: позиция, грейд, опыт
- Генерирует план интервью с темами и бюджетом вопросов
- Может корректировать план по ходу интервью (по запросу Router)

**Выход:**
```json
{
  "interview_plan": {
    "position": "Backend Developer",
    "target_grade": "Junior",
    "topics": [
      {"name": "Python basics", "priority": 1, "questions_budget": 3, "status": "pending"},
      {"name": "SQL", "priority": 2, "questions_budget": 2, "status": "pending"},
      {"name": "Django", "priority": 3, "questions_budget": 2, "status": "pending"},
      {"name": "Git", "priority": 4, "questions_budget": 1, "status": "pending"}
    ],
    "total_questions_limit": 8
  }
}
```

---

### 2. Interviewer (видимый агент)

**Задачи:**
- Ведет диалог, задает вопросы, реагирует на ответы
- Вежливо возвращает в русло интервью при off-topic
- Доставляет вопросы (НЕ генерирует сам — получает от Router)
- Адаптирует тон: дружелюбный → более строгий при расхождении с заявленным опытом

**Ограничения:**
- НЕ оценивает навыки
- НЕ проверяет факты
- НЕ решает какой вопрос задать следующим

---

### 3. Answer Analyzer (скрытый агент)

**Задачи:**
- Анализирует ответ кандидата на качество и полноту
- Оценивает уверенность (confidence score)
- Детектирует off-topic и попытки уйти от темы
- Определяет, содержит ли ответ потенциально ложные факты (флаг для Fact Checker)

**Выход:**
```json
{
  "answer_analysis": {
    "quality": "partial",
    "confidence_detected": 0.6,
    "completeness": 0.7,
    "off_topic": false,
    "needs_fact_check": true,
    "suspicious_claims": ["Python 4.0 удалит for"],
    "candidate_asked_question": false,
    "reasoning": "Кандидат знает основы, но сделал странное утверждение о Python 4.0"
  }
}
```

---

### 4. Fact Checker (скрытый агент)

**Задачи:**
- Проверяет технические факты из ответа кандидата
- Использует Web Search для верификации
- Определяет статус факта с уровнем уверенности
- Предоставляет корректную информацию для финального фидбэка

**⚠️ ПРИНЦИП ПРЕЗУМПЦИИ НЕВИНОВНОСТИ:**
> Факт считается ЛОЖНЫМ только если есть ДОСТОВЕРНОЕ ПОДТВЕРЖДЕНИЕ его ложности.
> Если проверить не удалось — факт помечается как "unverified" и НЕ влияет на оценку негативно.

**Источники знаний (приоритет):**
1. **Web Search API** — для проверки актуальных фактов
   - DuckDuckGo Search (бесплатный, без API ключа)
2. **Встроенные знания LLM** — только для ОЧЕВИДНЫХ фактов с высокой уверенностью (> 95%)

**Статусы фактов:**
| Статус | Условие | Влияние на оценку |
|--------|---------|-------------------|
| `verified_true` | Web Search подтвердил | ✅ Положительно |
| `verified_false` | Web Search опроверг с источником | ❌ Негативно |
| `unverified` | Не удалось проверить (API недоступен) | ⚪ Нейтрально |
| `llm_confident_true` | LLM уверен >95%, без Web Search | ✅ Положительно (с пометкой) |
| `llm_uncertain` | LLM не уверен, Web Search недоступен | ⚪ Нейтрально |

**Выход:**
```json
{
  "fact_check": {
    "verified_true": [
      {
        "claim": "Django использует MTV паттерн",
        "confidence": 0.98,
        "source": "docs.djangoproject.com",
        "verification_method": "web_search"
      }
    ],
    "verified_false": [
      {
        "claim": "Python 4.0 удалит for",
        "confidence": 0.99,
        "correct_info": "Python 4.0 не анонсирован. Циклы for — базовая конструкция языка, их удаление не планируется.",
        "source": "python.org, PEP process",
        "verification_method": "web_search"
      }
    ],
    "unverified": [
      {
        "claim": "В новой версии FastAPI добавили фичу X",
        "reason": "web_search_unavailable",
        "llm_assessment": "possibly_true",
        "note": "Не влияет на оценку — требует ручной проверки"
      }
    ]
  }
}
```

---

### 5. Evaluator (скрытый агент)

**Задачи:**
- Накапливает оценки по ходу всего интервью
- Обновляет "карту навыков" после каждого ответа
- Отслеживает подтвержденные навыки и пробелы
- Передает агрегированные данные Hiring Manager в конце

**Хранимое состояние:**
```json
{
  "evaluation": {
    "skills_confirmed": [
      {"skill": "Python basics", "confidence": 0.8, "evidence": ["turn 2", "turn 4"]},
      {"skill": "SQL joins", "confidence": 0.9, "evidence": ["turn 3"]}
    ],
    "skills_gaps": [
      {"skill": "async/await", "severity": "high", "failed_at": "turn 5"}
    ],
    "soft_skills": {
      "clarity": 0.8,
      "honesty": 0.9,
      "engagement": 0.7
    },
    "hallucinations_detected": 1,
    "off_topic_attempts": 0,
    "current_grade_estimate": "Junior",
    "grade_confidence": 0.75
  }
}
```

**Memory Heatmap (встроенная логика):**
- Каждой теме присваивается "температура": 🔥 hot (хорошо) → ❄️ cold (плохо)
- Темы с низкой температурой получают приоритет для повторных вопросов

---

### 6. Candidate Question Handler (скрытый агент)

**Задачи:**
- Активируется когда кандидат задает встречный вопрос
- Отвечает на вопросы о компании, позиции, испытательном сроке
- Не прерывает основной flow интервью

**Триггер:** `answer_analysis.candidate_asked_question == true`

**Выход:**
```json
{
  "candidate_question_response": {
    "question_detected": "Какие задачи будут на испытательном сроке?",
    "response": "На испытательном сроке вы будете работать над...",
    "return_to_interview": true
  }
}
```

---

### 7. Hiring Manager (скрытый агент, финальный отчет)

**Задачи:**
- Собирает итоговый фидбэк и вердикт
- Использует готовые данные от Evaluator (НЕ анализирует сырые логи)
- Формирует roadmap с конкретными рекомендациями
- Добавляет правильные ответы на проваленные вопросы (от Fact Checker)

**Активация:** команда "Стоп интервью" или исчерпан лимит вопросов

---

### 8. State Manager (инфраструктурный компонент)

**Задачи:**
- Хранит полное состояние сессии
- Предоставляет контекст агентам (последние N сообщений, план тем, оценки)
- Проверяет что вопросы не повторяются

**Хранимые данные:**
```json
{
  "session": {
    "session_id": "uuid",
    "candidate_profile": {...},
    "interview_plan": {...},
    "conversation_history": [...],
    "evaluation_state": {...},
    "questions_asked": ["list of question hashes"],
    "current_topic": "Python basics",
    "current_difficulty": "medium",
    "turn_count": 5
  }
}
```

---

### 9. Router (инфраструктурный компонент)

**Задачи:**
- Определяет какой агент должен работать следующим
- Управляет условными переходами в графе состояний
- Принимает решения об адаптивности (сложность вопросов)

**Логика маршрутизации:**
```
IF user_message == "стоп интервью" → Hiring Manager
ELSE IF answer_analysis.candidate_asked_question → Candidate Question Handler → Interviewer
ELSE IF answer_analysis.needs_fact_check → Fact Checker → Evaluator → Interviewer
ELSE → Evaluator → Interviewer
```

**Адаптивность (Confidence Ladder):**
- `quality == "excellent"` → повысить сложность, deeper follow-up
- `quality == "partial"` → уточняющий вопрос той же сложности
- `quality == "poor"` → дать подсказку, упростить следующий вопрос

---

### 10. Logger (инфраструктурный компонент)

**Задачи:**
- Асинхронно записывает все события в `interview_log.json`
- Не блокирует основной flow
- Форматирует логи в требуемом формате

---

## Граф состояний (State Graph для LangGraph)

```
                        ┌─────────────────┐
                        │      START      │
                        └────────┬────────┘
                                 ↓
                        ┌─────────────────┐
                        │  Topic Planner  │
                        └────────┬────────┘
                                 ↓
                        ┌─────────────────┐
                        │   Interviewer   │ (приветствие)
                        └────────┬────────┘
                                 ↓
           ┌─────────────────────────────────────────┐
           │            INTERVIEW_LOOP               │◄─────────────────┐
           └─────────────────────┬───────────────────┘                  │
                                 ↓                                      │
                        ┌─────────────────┐                             │
                        │  Wait for User  │                             │
                        └────────┬────────┘                             │
                                 ↓                                      │
              ┌──────────────────┴──────────────────┐                   │
              ↓                                      ↓                   │
     ┌─────────────────┐                   ┌─────────────────┐          │
     │ "Стоп" detected │                   │ Answer received │          │
     └────────┬────────┘                   └────────┬────────┘          │
              │                                     ↓                   │
              │                            ┌─────────────────┐          │
              │                            │ Answer Analyzer │          │
              │                            └────────┬────────┘          │
              │                                     ↓                   │
              │                    ┌────────────────┴────────────────┐  │
              │                    ↓                                  ↓  │
              │           ┌───────────────┐              ┌────────────┐ │
              │           │ needs_fact_   │              │ candidate_ │ │
              │           │ check?        │              │ question?  │ │
              │           └───────┬───────┘              └─────┬──────┘ │
              │                   ↓ YES                        ↓ YES   │
              │           ┌───────────────┐              ┌────────────┐ │
              │           │  Fact Checker │              │ Question   │ │
              │           └───────┬───────┘              │ Handler    │ │
              │                   ↓                      └─────┬──────┘ │
              │           ┌───────────────┐                    │       │
              │           │   Evaluator   │◄───────────────────┘       │
              │           └───────┬───────┘                            │
              │                   ↓                                    │
              │           ┌───────────────┐                            │
              │           │    Router     │ (адаптивность)             │
              │           └───────┬───────┘                            │
              │                   ↓                                    │
              │           ┌───────────────┐                            │
              │           │  Interviewer  │────────────────────────────┘
              │           └───────────────┘
              ↓
     ┌─────────────────┐
     │ Hiring Manager  │
     └────────┬────────┘
              ↓
     ┌─────────────────┐
     │      END        │
     └─────────────────┘
```

---

## Формат логирования (обязательный артефакт)

```json
{
  "team_name": "Team Name",
  "session_id": "uuid",
  "candidate_profile": {
    "name": "Алекс",
    "position": "Backend Developer",
    "target_grade": "Junior",
    "experience": "Пет-проекты на Django, немного SQL"
  },
  "turns": [
    {
      "turn_id": 1,
      "timestamp": "2024-01-15T10:30:00Z",
      "agent_visible_message": "Привет! Расскажи про свой опыт в Python.",
      "user_message": "Я джун, пишу код 3 месяца.",
      "internal_thoughts": {
        "answer_analyzer": {
          "quality": "partial",
          "confidence_detected": 0.5,
          "off_topic": false,
          "needs_fact_check": false,
          "reasoning": "Короткий ответ, нужно уточнить детали"
        },
        "evaluator": {
          "skill_update": null,
          "grade_estimate": "Junior",
          "recommendation": "Спросить про конкретные проекты"
        },
        "router": {
          "next_topic": "Python basics",
          "difficulty": "easy",
          "action": "ask_details"
        }
      }
    }
  ],
  "final_feedback": {
    "decision": {
      "grade": "Junior",
      "recommendation": "Hire",
      "confidence": 0.75
    },
    "technical_review": {
      "confirmed_skills": ["Python basics", "SQL"],
      "knowledge_gaps": [
        {
          "topic": "async/await",
          "correct_answer": "async/await в Python используется для асинхронного программирования..."
        }
      ]
    },
    "soft_skills": {
      "clarity": 0.8,
      "honesty": 0.9,
      "engagement": 0.7
    },
    "roadmap": [
      {"topic": "Асинхронное программирование", "resources": ["https://docs.python.org/3/library/asyncio.html"]},
      {"topic": "SQL оптимизация", "resources": ["https://use-the-index-luke.com/"]}
    ]
  }
}
```

---

## Реализация свойств Agentic Workflow

| Свойство | Реализация |
|----------|------------|
| **Role Specialization** | 6 специализированных агентов, каждый со строгими ограничениями |
| **Hidden Reflection** | Answer Analyzer + Fact Checker + Evaluator работают до ответа пользователю |
| **Context Awareness** | State Manager хранит историю и проверяет уникальность вопросов |
| **Adaptability** | Router применяет Confidence Ladder для изменения сложности |
| **Robustness** | Answer Analyzer детектирует off-topic, Fact Checker ловит галлюцинации |

---

## Web Search для Fact Checker

### Используемый API

| Сервис | Цена | Примечание |
|--------|------|------------|
| **DuckDuckGo** | Бесплатно | Через `duckduckgo-search` Python пакет |

### Стратегия поиска

```python
async def verify_fact(claim: str) -> FactCheckResult:
    # 1. Быстрая проверка через LLM (базовые факты)
    llm_check = await llm.check_basic_fact(claim)
    if llm_check.confidence > 0.95:
        return llm_check
    
    # 2. Web Search для неочевидных утверждений
    search_results = await duckduckgo.search(
        query=f"fact check: {claim}",
        max_results=3
    )
    
    # 3. LLM анализирует результаты поиска
    return await llm.analyze_search_results(claim, search_results)
```

---

## Минимальный стек

- **Python 3.11+**
- **LangGraph** — для реализации State Graph
- **LLM API**: OpenAI GPT-4o-mini
- **DuckDuckGo** — для Web Search (Fact Checker)
- **Pydantic** — для валидации структур данных

---

## Ожидаемые файлы

- `interview_log.json` — лог интервью в требуемом формате
- `ARCHITECTURE.md` — описание системы (этот документ)
- `src/` — исходный код системы

---

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         MULTI-AGENT SYSTEM                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Input (Position, Grade, Experience)                                     │
│           │                                                              │
│           ▼                                                              │
│  ┌─────────────────┐                                                     │
│  │  Topic Planner  │──────────────────────┐                              │
│  └─────────────────┘                      │                              │
│                                           ▼                              │
│                              ┌─────────────────────────┐                 │
│                              │     State Manager       │                 │
│                              │  ┌─────────────────┐    │                 │
│                              │  │ Interview Plan  │    │                 │
│                              │  │ Conversation    │    │                 │
│                              │  │ Evaluation      │    │                 │
│                              │  └─────────────────┘    │                 │
│                              └───────────┬─────────────┘                 │
│                                          │                               │
│  ┌───────────────────────────────────────┼───────────────────────────┐  │
│  │              INTERVIEW LOOP           │                           │  │
│  │                                       ▼                           │  │
│  │  User ──▶ Answer Analyzer ──▶ Fact Checker ──▶ Web Search API    │  │
│  │                │                      │              │            │  │
│  │                │                      ▼              │            │  │
│  │                │              Verified Facts ◀───────┘            │  │
│  │                │                      │                           │  │
│  │                ▼                      ▼                           │  │
│  │           Evaluator ◀─────────────────┘                           │  │
│  │                │                                                  │  │
│  │                ▼                                                  │  │
│  │            Router (адаптивность)                                  │  │
│  │                │                                                  │  │
│  │                ├──▶ Candidate Question Handler (если вопрос)      │  │
│  │                │                                                  │  │
│  │                ▼                                                  │  │
│  │          Interviewer ──▶ User                                     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─────────────────┐      ┌─────────────────┐                           │
│  │ Hiring Manager  │◀─────│    Evaluator    │  (при "Стоп интервью")    │
│  │ (final report)  │      │  (accumulated)  │                           │
│  └────────┬────────┘      └─────────────────┘                           │
│           │                                                              │
│           ▼                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                    Logger (async)                                │    │
│  │           Writes structured JSON to interview_log.json           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Edge Cases и их обработка

| Ситуация | Обработка |
|----------|-----------|
| Кандидат молчит > 30 сек | Interviewer мягко напоминает о вопросе |
| Ответ > 500 слов | Answer Analyzer выделяет ключевые тезисы |
| **Web Search недоступен** | **Презумпция невиновности**: факт → `unverified`, НЕ влияет на оценку |
| Исчерпан бюджет вопросов | Router завершает интервью → Hiring Manager |
| Кандидат задает вопрос | Candidate Question Handler → возврат к интервью |
| API timeout | Retry с LLM fallback (max 3 попытки) |
| LLM галлюцинирует при проверке | Требуется источник; без источника факт → `unverified` |

---

## Стратегия верификации фактов (Fact Verification Strategy)

### Принцип: "Виновен только при доказательствах"

```
┌─────────────────────────────────────────────────────────────────┐
│                    VERIFICATION FLOW                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Suspicious Claim                                                │
│         │                                                        │
│         ▼                                                        │
│  ┌─────────────────┐                                             │
│  │  Web Search     │──── Success ────▶ Source Found?            │
│  │  (DuckDuckGo)   │                        │                    │
│  └────────┬────────┘                   YES  │  NO                │
│           │                             │   │                    │
│      Timeout/Error                      ▼   ▼                    │
│           │                    ┌────────────────────┐            │
│           ▼                    │ Analyze source     │            │
│  ┌─────────────────┐           │ credibility        │            │
│  │  Fallback:      │           └─────────┬──────────┘            │
│  │  DuckDuckGo     │──── Success ───────▶│                       │
│  └────────┬────────┘                     │                       │
│           │                              ▼                       │
│      Timeout/Error              ┌────────────────────┐           │
│           │                     │ Credible source?   │           │
│           ▼                     └────────┬───────────┘           │
│  ┌─────────────────┐                YES  │  NO                   │
│  │  LLM Check      │                 │   │                       │
│  │  (confidence?)  │                 │   ▼                       │
│  └────────┬────────┘                 │  UNVERIFIED               │
│           │                          │  (нейтрально)             │
│      confidence                      │                           │
│        > 95%?                        ▼                           │
│       /     \              ┌──────────────────┐                  │
│     YES     NO             │ Source confirms? │                  │
│      │       │             └────────┬─────────┘                  │
│      ▼       ▼                  YES │ NO                         │
│   LLM_      UNVERIFIED              │  │                         │
│  CONFIDENT  (нейтрально)            ▼  ▼                         │
│  (положит.)              VERIFIED_TRUE  VERIFIED_FALSE           │
│                          (положит.)     (негативно)              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Критерии для статуса `verified_false`:

Факт считается ЛОЖНЫМ только при выполнении ВСЕХ условий:
1. ✅ Web Search вернул результаты
2. ✅ Найден авторитетный источник (официальная документация, известные ресурсы)
3. ✅ Источник явно противоречит утверждению кандидата
4. ✅ Confidence > 90%

### Что происходит с `unverified` фактами:

1. **Во время интервью:** Interviewer может мягко уточнить источник
2. **В финальном отчёте:** Выносятся в отдельную секцию "Требует проверки"
3. **Влияние на оценку:** НУЛЕВОЕ (ни положительное, ни отрицательное)

```json
{
  "final_feedback": {
    "technical_review": {
      "confirmed_skills": [...],
      "knowledge_gaps": [...],
      "unverified_claims": [
        {
          "claim": "В FastAPI 0.100 добавили новый роутер",
          "interviewer_note": "Не удалось проверить автоматически. Рекомендуется уточнить вручную.",
          "impact_on_grade": "none"
        }
      ]
    }
  }
}
```
